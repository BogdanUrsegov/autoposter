# Clickbot — кликер Telegram Stars

Бот: реклама после `/start`, кликер, рефералка, вывод звёзд и веб-админка.

## Быстрый старт

1. Python 3.11+
2. Скопируй `.env.example` в `.env` и заполни:

```
BOT_TOKEN=токен от @BotFather
ADMIN_IDS=твой telegram id
ADMIN_PASSWORD=пароль для веб-админки
```

3. Установка и запуск:

```
pip install -r requirements.txt
python run.py
```

Админка: [http://127.0.0.1:8000](http://127.0.0.1:8000)

В боте админам доступна команда `/admin`.

## Docker + PostgreSQL

Для Docker используется отдельная ветка `feature/v2-docker`.

1. Создай `.env` на основе `.env.docker.example` и заполни токен, админов, пароль и ключи сервисов.
2. Запусти:

```
docker compose up -d --build
```

3. Логи бота:

```
docker compose logs -f bot
```

Веб-админка будет доступна на порту `8000`.

PostgreSQL запускается отдельным контейнером и доступен боту по имени `postgres`. Данные PostgreSQL хранятся в Docker volume `postgres_data`, поэтому обычный перезапуск или пересоздание контейнеров базу не удаляет.

Для остановки:

```
docker compose down
```

Не используй `docker compose down -v`, если нужно сохранить базу: ключ `-v` удалит volumes вместе с данными PostgreSQL.

## Что умеет пользователь

- После `/start` приходит реклама из админки (HTML / Markdown / MarkdownV2, медиа, обычная или цветная кнопка, премиум-эмодзи через `tg-emoji`).
- Затем меню: **Кабинет**, **Кликер**, **Реферальная система**, **Вывод звёзд**.
- Кликер: награда по умолчанию `0.25` ⭐, кулдаун `0`, лимит `0` = без ограничений. Каждые 5 кликов — задание.
- Задания по приоритету: моё ОП → сервисы ОП → приветы/показы.
- Рефералка: `5` ⭐ когда друг заработал больше `10` ⭐, плюс `5%` с его заработка.
- Вывод: сумма ≥ минимума → 3 друга по ссылке → подписка на любой канал из ОП → инвойс на 3 ⭐ (проверка). Заявка уходит админу.

Личная ссылка: `https://t.me/БОТ?start=r123456789`

## Админка

- Статистика за день / неделю / всё время: люди, премы, клики, начисления, выводы, входящие звёзды, воронка, конверсии.
- Реклама на старте, офферы, рассылка.
- Реф-ссылки: переходы (каждый `/start`, даже повторный), уники, премы, оплаты, CPT/CPU/CPP, % конверсий. Цена закупа задаётся вручную.
- Заявки на вывод, пользователи, все лимиты и награды.

Рекламная ссылка: `https://t.me/БОТ?start=c_КОД`

Для проверки подписки бот должен быть админом в канале.

## Сервисы ОП

В коде заготовки `bot/services/providers.py` (subgram / flyer). Когда будут ключи API — подключаем без переписывания кликера.
